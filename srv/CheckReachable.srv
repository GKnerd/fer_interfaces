# CheckReachable.srv

PoseTarget[] targets                # chained; the first starts at the current state

---

Outcome outcome                     # OK, INVALID_GOAL, UNSUPPORTED, NOT_FOUND, UNREACHABLE, NO_PATH
uint32 failed_index                 # valid if outcome != OK
JointConfiguration[] configurations # one per target
